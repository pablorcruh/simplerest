spring:
  application:
    name: item
  datasource:
    url: jdbc:mysql://localhost:3306/item_system
    username: root
    password: example
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL57Dialect
    generate-ddl: true
